# generated with MKHProjGen
# https://github.com/maximkhatskevich/MKHProjGen
version: 1.2.1
configurations:
  Debug:
    type: debug
    profiles:
    - general:debug
    overrides:
      IPHONEOS_DEPLOYMENT_TARGET: 8.0
      DEVELOPMENT_TEAM: UJA88X59XP
      SWIFT_VERSION: 3.0
      VERSIONING_SYSTEM: apple-generic
      SWIFT_OPTIMIZATION_LEVEL: -Onone
  Release:
    type: release
    profiles:
    - general:release
    overrides:
      IPHONEOS_DEPLOYMENT_TARGET: 8.0
      DEVELOPMENT_TEAM: UJA88X59XP
      SWIFT_VERSION: 3.0
      VERSIONING_SYSTEM: apple-generic
targets:
  MyAwesomeFramework:
    platform: ios
    type: ":framework"
    sources:
    - Src
    configurations:
      Debug:
        overrides:
          IPHONEOS_DEPLOYMENT_TARGET: 8.0
          PRODUCT_BUNDLE_IDENTIFIER: khatskevich.maxim.MyAwesomeFramework
          INFOPLIST_FILE: Info/Fwk.plist
          SDKROOT: iphoneos
          TARGETED_DEVICE_FAMILY: 1,2
          DEFINES_MODULE: NO
          SKIP_INSTALL: YES
      Release:
        overrides:
          IPHONEOS_DEPLOYMENT_TARGET: 8.0
          PRODUCT_BUNDLE_IDENTIFIER: khatskevich.maxim.MyAwesomeFramework
          INFOPLIST_FILE: Info/Fwk.plist
          SDKROOT: iphoneos
          TARGETED_DEVICE_FAMILY: 1,2
          DEFINES_MODULE: NO
          SKIP_INSTALL: YES
  Tests:
    platform: ios
    type: ":bundle.unit-test"
    references:
    - MyAwesomeFramework
    sources:
    - Tst
    configurations:
      Debug:
        overrides:
          IPHONEOS_DEPLOYMENT_TARGET: 8.0
          PRODUCT_BUNDLE_IDENTIFIER: khatskevich.maxim.MyAwesomeFramework.Tst
          INFOPLIST_FILE: Info/Tst.plist
          FRAMEWORK_SEARCH_PATHS: $(inherited) $(BUILT_PRODUCTS_DIR)
      Release:
        overrides:
          IPHONEOS_DEPLOYMENT_TARGET: 8.0
          PRODUCT_BUNDLE_IDENTIFIER: khatskevich.maxim.MyAwesomeFramework.Tst
          INFOPLIST_FILE: Info/Tst.plist
          FRAMEWORK_SEARCH_PATHS: $(inherited) $(BUILT_PRODUCTS_DIR)
variants:
  $base:
    abstract: true
  Main: